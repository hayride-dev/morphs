package hayride:mcp-server;

let datetime = new hayride:datetime@0.0.1 {...};

let tools = new hayride:default-tools@0.0.1 {
  datetime: datetime.datetime,
  ...
};

// let auth = new hayride:mcp-ory-auth@0.0.1 {...};

let server = new hayride:mcp-http-server@0.0.1 {
  tools: tools.tools,
  //auth: auth.auth,
  ...
};

// Export the server handler and config
export server...;
